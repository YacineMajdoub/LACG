# Agentic Codegen

A modular repository converted from a Colab notebook for agentic code generation, documentation retrieval, and validation.

## Setup
1. Install dependencies: `pip install -r requirements.txt`
2. Copy `.env.example` to `.env` and add your API keys.
3. Run scripts in the `scripts/` directory.
